<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="@string/pref_sort_by_popularity_desc"
        android:entries="@array/pref_sort_by_options"
        android:entryValues="@array/pref_sort_by_values"
        android:key="@string/pref_sort_by_key"
        android:title="@string/pref_sort_by_label" />

    <ListPreference
        android:key="@string/pref_movie_language_key"
        android:title="@string/pref_movie_language_label"/>

</PreferenceScreen>
